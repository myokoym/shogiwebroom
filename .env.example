# Environment Configuration Template
# Copy this file to .env and update the values as needed

# Application Environment
NODE_ENV=production
PORT=3000

# Redis Configuration (Upstash Redis)
# Replace with your actual Upstash Redis URL
REDIS_URL=redis://default:YOUR_PASSWORD@YOUR_HOST.upstash.io:YOUR_PORT

# Optional: Redis Connection Options
REDIS_MAXRETRIESPERREQUEST=3
REDIS_RETRY_DELAY_ON_FAILURE=100
REDIS_ENABLE_OFFLINE_QUEUE=false

# Application Settings
# Optional: Custom host settings (usually not needed on Fly.io)
# HOST=0.0.0.0

# Optional: Session/Security Settings
# SESSION_SECRET=your-session-secret-here

# Optional: Logging Level
# LOG_LEVEL=info

# Development Settings (only used in development)
# NODE_OPTIONS=--openssl-legacy-provider